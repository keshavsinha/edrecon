# EDRecon requirements
# Install with: pip install -r requirements.txt

# --- required -------------------------------------------------------------
PyYAML>=6.0            # scope file + knowledge base parsing
requests>=2.31.0       # HTTP modules, certificate transparency
dnspython>=2.4.0       # DNS reconnaissance

# --- recommended ----------------------------------------------------------
beautifulsoup4>=4.12.0 # form analysis in the web phase
python-whois>=0.9.0    # WHOIS registration lookups
urllib3>=2.0.0         # TLS warning suppression for lab self-signed certs
